exclude_paths:
 - .cache/
 - meta/
 - molecule/
 - .tox
skip_list:
 - '106'  # Role name {} does not match ``^[a-z][a-z0-9_]+$`` pattern
 - experimental  # all rules tagged as experimental
 - galaxy[no-changelog]
